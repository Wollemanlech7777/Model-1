# Site Companion Web

Next.js UI for Site Companion. Uses the single Grok bot avatar definition as the chat companion status character.

```bash
npm install
npm run dev
```

Open http://localhost:3000
